# AI SpendWise Backend 의존성
# 설치: pip install -r app/requirements.txt
# (현재 venv 기준 버전 고정 — AWS EC2 배포 시 동일 환경 재현용)

fastapi==0.135.3
uvicorn==0.42.0
python-multipart==0.0.24
python-dotenv==1.2.2

# DB
SQLAlchemy==2.0.48
PyMySQL==1.1.2

# 인증 — bcrypt는 4.x부터 passlib 1.7.4와 호환되지 않으므로 4 미만으로 고정 (CLAUDE.md 규칙)
passlib==1.7.4
bcrypt==3.2.2
PyJWT==2.12.1

# 스키마 검증 (EmailStr 사용으로 email-validator 필요)
pydantic==2.12.5
email-validator==2.3.0

# OCR / AI — google-generativeai(deprecated)가 아닌 google-genai를 사용 (CLAUDE.md 규칙)
google-cloud-vision==3.13.0
google-genai==2.5.0
